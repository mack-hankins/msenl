<div class="sub-header">
    <div class="container">
        <h1>{{ isset($title) ? $title : 'MSENL' }}</h1>

        <p>
            <span>{{ isset($description) ? $description : '' }}</span>
        </p>
    </div>
</div>
